*本质是一个“留言墙

*需要一个”公共记事本“存内容，在网页打开的时候读取保存就好——原来如此，这些云数据平台帮我把后端数据库全部都写好封装，我们直接调用；简单得多（之前的课设是为了让我们会底层的技术，工作对应岗位用的那种）
	-啥意思？腾讯文档的api永久失效了……上当——————说api不能这样直接读取，尝试自己写server做中转/换一个免费数据库——解析是说，腾讯文档不允许“跨域访问（浏览器不允许两个网页之间读数据）”，那么需要搭建一个服务器程序，因为文档的api好像是允许的
设计几种动物形象
	-如果 404：说明 key 或 gid 配错，或文档权限未开启（公开）
405（OPTIONS append）：
说明服务端不接受跨域预检。通过你自己的中转就消除（中转服务返回 Access-Control-Allow-Origin:* 你前端就能请求）

*把网页代码部署到github上生成公网可访问的链接——🆗



*自动生成星星：
JS 负责：（容器）
	生成 150 个小盒子 
	随机大小
	随机位置
CSS 负责： （样式）
	白色
	圆形
	闪烁动画